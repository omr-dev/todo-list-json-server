{
  "name": "todo-list-json-server",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "json-server-start": "npx json-server --watch ./data/todos-db.json --port 3002"
  },
  "devDependencies": {
    "vite": "^2.9.0"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "bootstrap": "^5.1.3",
    "json-server": "^0.17.0"
  }
}
